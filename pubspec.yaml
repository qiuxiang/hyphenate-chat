name: hyphenate_chat
description: A new flutter plugin project.
version: 0.0.1
author: 7c00 <i@7c00.cc>
homepage: https://github.com/qiuxiang/hyphenate-chat

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: hyphenate_chat
        pluginClass: HyphenateChatPlugin
